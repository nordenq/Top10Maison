---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import SEO from "../components/SEO.astro";

const {
  title,
  description,
  canonical,
  schema = [],
  ogImage,
  metaRefresh
} = Astro.props as {
  title: string;
  description: string;
  canonical: string;
  schema?: Array<Record<string, unknown>>;
  ogImage?: string;
  metaRefresh?: string;
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    {metaRefresh ? <meta http-equiv="refresh" content={metaRefresh} /> : null}
    <meta name="theme-color" content="#F9FAFB" />
    <SEO title={title} description={description} canonical={canonical} schema={schema} ogImage={ogImage} />
    <meta name="google-adsense-account" content="ca-pub-3680083929895808" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="sitemap" type="application/xml" href="/sitemap.xml" />
    <link rel="robots" href="/robots.txt" />
    <script
      async
      src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3680083929895808"
      crossorigin="anonymous"
    ></script>
    <script>
      window.addEventListener('DOMContentLoaded', function () {
        if (!localStorage.getItem('cookieConsent')) {
          const banner = document.createElement('div');
          banner.className = 'cookie-banner';
          banner.innerHTML = `
            <p class="cookie-banner__text">
              This site uses cookies to improve performance and serve personalized ads.
              <a href="/legal/">Learn more</a>.
            </p>
            <div class="cookie-banner__actions">
              <button type="button" data-consent="accept">Got it!</button>
              <button type="button" data-consent="decline" class="cookie-banner__decline">Decline</button>
            </div>
          `;
          const btn = banner.querySelector('[data-consent="accept"]');
          const declineBtn = banner.querySelector('[data-consent="decline"]');
          btn?.addEventListener('click', function () {
            localStorage.setItem('cookieConsent', 'true');
            banner.remove();
          });
          declineBtn?.addEventListener('click', function () {
            localStorage.setItem('cookieConsent', 'false');
            banner.remove();
          });
          document.body.appendChild(banner);
        }
      });
    </script>
  </head>
  <body class="min-h-screen flex flex-col">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
