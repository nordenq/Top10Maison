---
const { title, intro } = Astro.props as { title: string; intro: string };
---

<section class="section">
  <div class="mx-auto w-full max-w-6xl">
    <p class="text-xs font-semibold uppercase tracking-[0.3em] text-ink/50">Top10Maison Guide</p>
    <h1 class="mt-3 text-3xl font-semibold md:text-4xl">{title}</h1>
    <p class="mt-4 text-base text-ink/70">{intro}</p>
  </div>
</section>
