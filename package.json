{
  "name": "top10maison",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-toplists.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check:google-fonts": "node scripts/check-google-fonts.js",
    "build:ci": "node scripts/generate-toplists.mjs && astro build && npm run check:google-fonts",
    "postbuild": "node scripts/inline-critical.js"
  },
  "dependencies": {
    "astro": "^4.11.0"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "autoprefixer": "^10.4.23",
    "critters": "^0.0.20",
    "tailwindcss": "^3.4.19"
  }
}
